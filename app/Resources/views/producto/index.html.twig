{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
      <div class="col-2">
      <br><br>
      <ul>
          <li>
              <h4> <a href="{{ path('producto_new') }}">Crear nuevo Producto</a></h4>
          </li>
      </ul>
      </div>
      <div class="col-10">
      <h1 class="page header">Listado de Productos</h1>
      <table class="table table-striped">
      <thead>
      <tr>
      <th>ID</th>
      <th>NOMBRE</th>
      <th>AÑO</th>
      <th>OPCIONES</th>

      </tr>
      </thead>
      <tbody>
      {% for producto in productos %}
      <tr>
      <th scope="row">{{ producto.id }}</th>
      <td>{{ producto.nombre }}</td>
      <td>{{ producto.year }}</td>
      <td>

      <a href=" {{ path('producto_edit', { 'id': producto.id }) }}">Editar |</a>
      <a href="{{ path('producto_show', { 'id': producto.id }) }}">Mostrar |</a>
      <a href="{{ path('producto_delete', { 'id': producto.id }) }}">Eliminar</a>

      </td>
      </tr>
      {% endfor %}
      </tbody>
      </table>
      </div>
    </div>





{% endblock %}
