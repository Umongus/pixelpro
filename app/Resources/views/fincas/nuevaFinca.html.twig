{% extends 'base.html.twig' %}
{% block body %}
<div class="row">
<div class="col-1">

</div>
<div class=”col-4”>
<br>
<h2 class="page-header">Añadir nueva finca:</h2>

{{form_start(form)}}
  <fieldset>
  <br>
  <div>
  <strong>{{ form_label(form.nombre) }}</strong><br>
  <span>{{ form_widget(form.nombre) }}</span>
  </div><br>
  
  <div>
  <strong>{{ form_label(form.superficieOlivar) }}</strong><br>
  <span>{{ form_widget(form.superficieOlivar) }}</span>
  </div><br>

  <div>
  <strong>{{ form_label(form.superficieCalma) }}</strong><br>
  <span>{{ form_widget(form.superficieCalma) }}</span>
  </div><br>
  <div>
  <strong>{{ form_label(form.cultivoPrincipal) }}</strong><br>
  <span>{{ form_widget(form.cultivoPrincipal) }}</span>
  </div><br>
  </fieldset>
{{ form_end(form) }}
</div>
</div>
{% endblock %}
