{% extends 'base.html.twig' %}

{% block body %}

<div class="row">
  <div class="col-3">


  </div>
  <div class="col-9">

  
  <h2 class="page header">Registros de E/S: Cosecha </h2>

  <h2> <a href="{{ path('ES', { 'dia': 'Anterior' }) }}">Cosecha Anterior </a>Cosecha: <strong> ------ </strong> <a href="#">Cosecha Siguiente</a></h2>
  <hr>
  <h3> <strong>Resumen (Entrada): </strong> </h3>
  <h3> Gordal =<strong> 0 </strong>| Manzanilla = <strong> 0 </strong>| Molino = <strong> 0 </strong>| G.Morada = <strong> 0 </strong>| M.Morada = <strong> 0 </strong>| Z.Morada = <strong> 0 </strong></h3>


  <table class="table table-striped table-sm">
  <thead>
  <tr>
  <th>ID</th>
  <th>FECHA</th>
  <th>KILOS</th>
  <th>VARIEDAD</th>
  <th>FINCA</th>
  <th>CUADRILLA</th>
  <th>ENTIDAD</th>
  <th>COSECHA</th>


  </tr>
  </thead>
  <tbody>
  {% for parte in partes %}
  <tr>
  <th scope="row">{{ parte.id }}</th>
  <td>{{ parte.fecha|date('d-m-Y') }}</td>
  <td>{{ parte.peso }}</td>
  <td>{{ parte.variedad }}</td>
  <td>{{ parte.finca }}</td>
  <td>{{ parte.cuadrilla }}</td>
  <td>{{ parte.entidad }}</td>
  <td>{{ parte.producto }}</td>
  <td>
  <a href=" {{ path('entradasalida_edit', { 'id': parte.id, 'procede': 'EntradasSalidas' }) }}">Editar |</a>
  <a href="{{ path('entradasalida_show', { 'id': parte.id }) }}">Mostrar |</a>
  <a href="{{ path('entradasalida_delete', { 'id': parte.id }) }}">Eliminar</a>
  </td>
  </tr>
  {% endfor %}
  </tbody>
  </table>
  <br>
  <p>El contenido de desastre vale: 0</p>
  </div>
</div>

{% endblock %}
